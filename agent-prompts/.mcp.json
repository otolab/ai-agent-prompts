{
  "mcpServers": {
    "mode-controller": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@otolab/mcp-mode-controller",
        "--modes-path",
        "${CLAUDE_PLUGIN_ROOT}/prompts/modes,${CLAUDE_PLUGIN_ROOT}/../products"
      ]
    }
  }
}